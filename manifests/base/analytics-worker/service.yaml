apiVersion: v1
kind: Service
metadata:
  name: analytics-worker
  labels:
    app: analytics-worker
spec:
  selector:
    app: analytics-worker
  ports:
    - name: http
      port: 8000
      targetPort: http
