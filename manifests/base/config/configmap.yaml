apiVersion: v1
kind: ConfigMap
metadata:
  name: snapshot-config
data:
  # Update to the real domain/IP once deployed -- shown back to users as
  # the short_url in create-link responses.
  base-url: "http://localhost:8080"
  demo-mode: "false"
