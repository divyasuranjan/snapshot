apiVersion: v1
kind: Service
metadata:
  name: shortlink-api
  labels:
    app: shortlink-api
spec:
  selector:
    app: shortlink-api
  ports:
    - name: http
      port: 8080
      targetPort: http
